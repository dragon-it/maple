import {
  getGuildBasicInformation,
  getGuildRanking,
  getOguildId,
  getGuildMembers,
} from "./api";

const SearchGuildFetch = async (
  worldName,
  guildName,
  setResult,
  setLoading,
  setError
) => {
  if (worldName) {
  }
};
